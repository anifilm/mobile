// 타입 별칭
typealias MyInt = Int
typealias YourInt = Int
typealias MyDouble = Double

let age: MyInt = 100
var year: YourInt = 2021

// MyInt와 YourInt 모두 Int이기 때문에 같은 타입으로 취급
your = age

let month: Int = 7  // 물론 기존의 Int도 사용 가능
let precentage: MyDouble = 99.9  // Int 이외의 다른 자료형도 모두 별칭 사용 가능
